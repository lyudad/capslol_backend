enum JobResponse {
  HAS_SKILLS = 'job must contain at least one skill',
  INVALID_OWNER_ID = 'invalid ownerId',
  INVALID_CATEGORY_ID = 'invalid ownerId',
  INVALID_SKILL_ID = 'invalid skillId ',
}

export default JobResponse;
